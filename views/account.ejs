<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://kit.fontawesome.com/11d589870a.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href='/signup.css'/>
    <link rel="stylesheet" href='/account.css'/>
    <title><%=data.name%></title>
</head>
<body>
    <div class="account-container">
        <div class="title">
            Update your account
        </div>
        <div class="content">
            <div class="input-field">
                <i class="fas fa-user"></i>
                <input class="input name" type="text" placeholder="Name" value="<%=data.name%>"/>
            </div>
            <div class="input-field">
                <i class="fas fa-graduation-cap"></i>
                <select class='input role'>
                    <option disabled>Role</option>
                    <%if(data.role==='Teacher'){%>
                        <option selected>Teacher</option>
                        <option>Student</option>
                    <%}else{%>
                        <option>Teacher</option>
                        <option selected>Student</option>
                    <%}%>
                </select>
            </div>
            <div class="input-field disabled">
                <i class="fas fa-envelope"></i>
                <input class="input email" type="email" value="<%=data.email%>" placeholder="Email" oninput="validEmail(this)" onblur="checkme(this.value)" disabled/>
            </div>
            <div class="button-container">
                <div class="error-message">
                    
                </div>
                <div class="button" onclick="submit(this)">
                    Update
                </div>
                <div class="loading">
                    <i class="fas fa-circle-notch fa-spin"></i>
                </div>
            </div>
            
        </div>
    </div>
</body>
</html>